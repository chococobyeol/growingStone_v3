# Synthesis 구현 TODO

작성일: 2026-02-24  
목적: `docs/SYNTHESIS_DESIGN.md` 기준으로 실제 구현 작업을 바로 진행할 수 있는 체크리스트 제공.

---

## 1) 백엔드 RPC 구현

- [x] `public.synthesize_stone` RPC 생성 (`backend/schema.sql`)
- [x] 입력 파라미터 확정: `elements_ratio`, `temperature(1~5)`, `pressure(low|high)`, `batch_count`
- [x] `auth.uid()` 인증 및 사용자 소유 데이터 접근 검증
- [x] 비율 매칭 로직 구현 (정규화 + 거리 계산 1종으로 고정)
- [ ] PM 필터 적용 (`temperature+pressure -> pm_ids` 교집합)
- [ ] PM fallback 구현 (인접 구간 확대 -> 최종 PM 무시)
- [x] 필수 원소 누락 시 실패 처리 (`MISSING_ELEMENT`)
- [x] 한계 원소 기준 `batches` 계산
- [x] 1회분 총량 `300` 고정 계산 적용
- [x] 원소량 소수점 6자리(`numeric(12,6)`) 처리
- [x] 트랜잭션 처리: 원소 차감 + 스톤 차감 + `stones` INSERT
- [x] 응답 스키마 고정: 성공/실패 코드 및 메시지 일관화

## 2) 합성 계산/룰 정리

- [ ] 온도·압력 -> PM 매핑 테이블 서버 상수화
- [x] `batch_count` v1 정책 고정 (초기 1 우선 또는 다회 지원)
- [x] 합성 비용 공식 v1 확정
- [x] 과잉 원소 미소모 정책 적용
- [x] `stones.dna.seed` 생성/저장 규칙 확정

## 3) 클라이언트 UI (`LabUI`) 구현

- [x] 합성 탭 placeholder 제거
- [x] 원소 선택 UI (선택/해제 및 선택 순서 유지)
- [x] 비율 조절 UI (슬라이더 + 정수 입력 동기화)
- [x] 온도/압력 입력 UI 추가
- [x] 합성 전 사전검증(보유 원소/스톤/입력값)
- [x] 합성 버튼 활성/비활성 상태 처리
- [x] 합성 RPC 호출 및 로딩 상태 표시
- [x] 실패 메시지 코드별 UI 처리
- [x] 성공 결과 UI 표시(광물명, 소비 원소, 배치 비율)
- [x] 성공 후 인벤토리/스톤/보유 돌 데이터 동기 갱신

## 4) 게임 상태 연동

- [ ] `GameManager` 합성 후 데이터 갱신 함수 추가
- [ ] `Main`에서 새로 생성된 돌 표시 연동
- [ ] 단일 돌 구조 유지 여부 또는 다중 돌 구조 전환 결정
- [ ] 다중 돌 지원 시 현재 성장 대상 선택 흐름 설계

## 5) 테스트/검증

- [ ] 정상 케이스: 충분한 원소 + PM 매칭 + 합성 성공
- [ ] 실패 케이스: 필수 원소 누락, 스톤 부족, 잘못된 입력값
- [ ] 경계 케이스: 소수 6자리, 매우 작은 배치 비율
- [ ] PM 후보 0개 fallback 동작 검증
- [ ] 트랜잭션 롤백 검증(중간 실패 시 데이터 불일치 방지)
- [ ] 중복 클릭/동시 요청 방어 확인

## 6) 문서 동기화

- [ ] `docs/SYNTHESIS_DESIGN.md`와 RPC 실제 시그니처 동기화
- [ ] `docs/TODO.md` 합성 관련 항목 단계별 완료 처리
- [ ] 에러 코드 표준 문서화(`MISSING_ELEMENT`, `INSUFFICIENT_STONE` 등)

---

## 권장 구현 순서

1. `synthesize_stone` RPC 뼈대 + 트랜잭션  
2. 비율 매칭 + PM 필터 + fallback  
3. LabUI 합성 탭 입력/검증 UI  
4. 성공/실패 응답 연결 및 상태 동기화  
5. 테스트 케이스 고정 및 문서 업데이트
