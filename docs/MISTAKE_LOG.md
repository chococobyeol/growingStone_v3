## [2026-02-24 00:00] 합성 주기율표 숨김 처리 오적용 수정

- 증상: 합성 탭에서 란타넘/악티늄 행이 기본 숨김 처리되어 사용자 요구와 다르게 표시됐다.
- 원인: 가독성 개선 작업 중 "기본 숨김"을 하드코딩하고, 요구사항(스크롤 접근) 재확인을 누락했다.
- 해결: 합성 탭 9~10행 숨김 로직을 제거하고, 토글 문구를 "원소 리스트/조건" 기준으로 정렬했다.
- 재발 방지: UI 변경 시 요구사항 핵심 문구(기본 숨김 금지/스크롤/접힘 조건)를 체크리스트로 먼저 고정한다.
- 검증: `LabUI.gd`에서 합성 주기율표 행 범위가 1~10으로 유지되는지와 토글 텍스트 동작 일치 여부를 코드 검토로 확인했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 합성 화면 잘림 및 토글 동작 불일치 수정

- 증상: 합성 탭에서 화면 높이가 과도하게 커져 상하가 잘리고, 토글을 눌러도 주기율표가 접히지 않아 선택 원소 목록이 좁게 보였다.
- 원인: 합성 레이아웃 높이를 고정에 가까운 큰 최소값으로 설정했고, 토글 동작을 "아래에 추가 표시" 방식으로 구현해 요구사항(표 접힘 전환)을 어겼다.
- 해결: 토글 상태에 따라 `inventory_panel`과 `synth_root`를 상호 전환하도록 수정하고, 두 영역 높이를 뷰포트 비율 기반으로 재계산하도록 변경했다.
- 재발 방지: 합성 UI 변경 시 "기본 화면/토글 후 화면" 두 상태를 분리한 체크리스트로 검증하고, 고정 최소 높이 대신 뷰포트 비율 기반으로 먼저 설계한다.
- 검증: `ReadLints` 전체 검사에서 오류 없음 확인, `LabUI.gd`에서 토글 문구/표시 전환/높이 계산 함수 연결을 코드 검토로 확인했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 합성 선택 순서/주기율표 스케일 불일치 수정

- 증상: 선택 원소 목록이 원소명 정렬로 표시되어 클릭 순서가 깨졌고, 합성 탭 주기율표 셀/폰트 크기가 뽑기 탭과 달랐다.
- 원인: 리스트 렌더링에서 `sort()`를 사용했고, 탭별 셀 크기 상수를 분리해 문서 기준(순서 유지·공통 표시) 검증을 놓쳤다.
- 해결: `selected_order` 배열로 클릭 순서를 별도 관리하고, 주기율표 크기/스타일 적용을 `_apply_periodic_table_style()` 공통 함수로 통합했다.
- 재발 방지: 문서의 UI 고정조건(순서 유지/공통 스케일)은 코드에 상태 변수와 공통 함수로 강제하고, 정렬 호출 여부를 변경 시마다 점검한다.
- 검증: `ReadLints`에서 오류 없음 확인, `LabUI.gd`에서 선택/해제/제거/재고정리/합성성공 시 순서 배열 동기화와 공통 스타일 함수 적용을 코드 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 합성 선택 패널 구조 미스매치 수정

- 증상: 합성 선택 화면이 스케치와 다르게 우측이 과도하게 크고, 좌측 리스트에서 슬라이더/백분율/소모량이 빠져 비율 조절 UX가 불완전했다.
- 원인: 초기 구현에서 `SpinBox` 중심으로만 구성하고, 패널 너비 비율을 우측 확장형으로 둬 문서/스케치의 정보 배치를 반영하지 못했다.
- 해결: 좌측 패널 폭을 확장하고 우측 패널을 컴팩트하게 축소했으며, 선택 원소 행에 정수 입력+슬라이더+백분율+소모량을 함께 표시하도록 재구성했다.
- 재발 방지: UI 구현 시 스케치의 핵심 구성 요소(좌/우 비율, 행 구성 요소)를 코드 체크리스트로 먼저 확정하고 누락 항목이 있으면 구현 완료로 판단하지 않는다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, 선택 리스트 재렌더링 경로에서 순서 유지/비율 입력/슬라이더 입력이 공통 갱신 함수로 연결됨을 코드 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 합성 패널 좌우 역할 분리 및 백분율 슬라이더 보정

- 증상: 합성 화면에서 좌우 패널 역할이 섞여 보이고, 백분율 슬라이더가 좌측에 몰려 보여 시각적으로 비율 조절이 직관적이지 않았다.
- 원인: 우측 패널이 확장형으로 남아 구조가 흐려졌고, 슬라이더 값이 정수비 기준으로 연결되어 백분율 표시와 위치 체감이 어긋났다.
- 해결: 우측 패널을 컴팩트 박스로 축소해 조건/미량/합성만 배치하고, 슬라이더를 1~99 백분율 입력으로 전환해 현재 비율 퍼센트와 핸들 위치를 일치시켰다.
- 재발 방지: 슬라이더가 백분율을 표시할 때는 내부값도 퍼센트 도메인으로 맞추고, 정수비는 변환 단계에서만 계산하도록 분리한다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, 슬라이더 시그널이 `_on_ratio_slider_percent_changed`로 연결되고 우측 패널이 shrink 모드로 동작하도록 코드 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 합성 우측 영역 3요소 분리 및 미량원소 팝업화

- 증상: 우측 영역이 큰 패널 중심으로 보이고, 요구한 형태(온도/압력 박스 + + 버튼 + 합성 버튼)와 달라 구성 의도가 전달되지 않았다.
- 원인: 기존 컨트롤을 같은 패널 내부의 행으로만 조정해 구조 자체를 분리하지 못했고, + 버튼도 즉시추가 로직이라 선택 팝업 흐름이 없었다.
- 해결: 우측을 액션 행 중심으로 재배치해 3요소를 독립 배치하고, + 버튼 누르면 미량원소 선택 팝업(Grid 버튼)에서 선택하도록 변경했다.
- 재발 방지: 사용자가 스케치로 "구성"을 지시한 경우, 간격/크기 조정 전에 컨테이너 계층과 인터랙션 흐름(버튼 클릭 후 동작)을 먼저 확정한다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, `_build_trace_picker_popup`/`_on_trace_element_chosen` 연결 및 우측 3요소 텍스트/배치 코드를 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 합성 패널 중앙 정렬 요구 미반영 수정

- 증상: 왼쪽 리스트 영역이 지나치게 넓고, 오른쪽 컨트롤 묶음이 상단에 붙어 보여 스케치의 중앙 배치와 달랐다.
- 원인: 우측 패널을 `SHRINK_BEGIN`으로 두고 컨텐츠를 직접 붙여 배치해, 남는 높이에서 중앙 정렬이 일어나지 않았다.
- 해결: 좌측 최소 폭을 줄이고, 우측 패널을 전체 높이 확장 후 `CenterContainer`로 감싸 우측 컨트롤 묶음을 세로/가로 중앙에 배치했다.
- 재발 방지: 스케치에 중앙 배치 요구가 있으면 컨테이너 레벨에서 `CenterContainer`를 먼저 적용하고, 축소/확장은 그 다음에 조정한다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, 좌우 폭(`580/440`)과 우측 `CenterContainer`/`ALIGNMENT_CENTER` 적용을 코드 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 탭 전환 시 합성 UI 잔존 버그 수정

- 증상: 합성 탭에서 열린 선택 리스트/합성 UI가 원소 뽑기 탭으로 이동해도 계속 보였다.
- 원인: `_update_synth_layout()`가 `current_tab != "synthesize"`에서 즉시 리턴하면서 `synth_root`와 팝업을 숨기는 동작이 빠졌다.
- 해결: 합성 탭이 아닐 때 `synth_root.visible = false`, `trace_picker_popup.hide()`를 강제 실행하도록 수정했다.
- 재발 방지: 탭 기반 UI는 각 탭 진입 시 표시 상태뿐 아니라 탭 이탈 시 숨김 상태를 명시적으로 처리한다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, `_update_synth_layout` 초반 분기에서 강제 숨김 코드 반영을 확인했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 합성 우측 스케치 레이아웃 및 슬라이더 드래그 갱신 수정

- 증상: 우측 UI가 스케치(온도/압력/미량원소 세로 박스 + 하단 합성 버튼)와 달랐고, 비율 슬라이더가 드래그 중 거의 움직이지 않았다.
- 원인: 우측을 가로 액션행 중심으로 구성해 스케치 형태를 깨뜨렸고, 슬라이더 `value_changed`에서 리스트 전체 재빌드를 수행해 드래그 이벤트가 끊겼다.
- 해결: 우측을 세로 박스 3개 + 하단 중앙 합성 버튼 구조로 재구성하고, 슬라이더는 드래그 중 미리보기만 갱신/드래그 종료 시 최종 재빌드로 변경했다.
- 재발 방지: 드래그 입력 위젯은 실시간 입력 중 무거운 재렌더링을 금지하고, 커밋 시점(드래그 종료) 이벤트를 별도로 사용한다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, 슬라이더 연결이 `value_changed(미리보기)` + `drag_ended(최종 반영)`로 분리된 것을 코드 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 온도/압력 토글 및 미량원소 원소+농도 입력 구조 반영

- 증상: 온도/압력이 드롭다운이라 스펙의 세그먼트/토글 UX와 달랐고, 미량원소는 농도 선택 없이 단일 추가만 가능했다.
- 원인: 기존 구현 편의상 `OptionButton` 중심으로 유지되어 PRD/UI 요청(온도 5분할, 압력 2토글, 원소+농도 입력)을 충분히 반영하지 못했다.
- 해결: 온도를 1~5 세그먼트 버튼으로, 압력을 저압/고압 토글 버튼으로 교체하고, 미량원소 팝업에 원소+농도(1~3) 선택과 칩 표시/삭제 기능을 추가했다.
- 재발 방지: 입력 UI 요구가 구체적일 때(세그먼트/토글/칩), 기존 위젯 재사용보다 상태 모델부터 요구 형태로 바꾼다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, 합성 요청이 `selected_temperature`, `selected_pressure`, `trace_entries`를 사용하는 코드 경로를 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`

## [2026-02-24 00:00] 미량원소 검증 누락(중복/보유량/RPC 전달) 수정

- 증상: 미량원소가 중복 추가 가능하고 보유하지 않은 원소도 선택 가능했으며, trace 정보가 합성 RPC body에 반영되지 않았다.
- 원인: trace UI 기능을 빠르게 붙이면서 입력 검증과 서버 전달 경로를 누락했다.
- 해결: 원소당 1개 엔트리 제한, 최대 3종 제한, 농도(1~3) 기준 보유량 검증, 인벤토리 갱신 시 trace prune, 합성 RPC에 `trace_elements` 포함을 반영했다.
- 재발 방지: UI 입력 기능 추가 시 `입력 제한/보유량 검증/RPC payload 반영` 3항목을 체크리스트로 고정한다.
- 검증: `ReadLints`에서 `LabUI.gd` 오류 없음 확인, `_on_trace_add_confirmed`, `_prune_trace_entries`, `_on_synthesize_pressed`의 검증/전달 경로를 코드 검토했다.
- 관련 파일: `LabUI.gd`, `docs/TODO.md`, `docs/MISTAKE_LOG.md`
